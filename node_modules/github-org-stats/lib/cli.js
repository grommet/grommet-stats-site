'use strict';

var _GithubOrgStats = require('./GithubOrgStats');

var _GithubOrgStats2 = _interopRequireDefault(_GithubOrgStats);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var GITHUB_TOKEN = process.env.GITHUB_TOKEN;
var GITHUB_ORGANIZATION = process.env.GITHUB_ORGANIZATION;

var githubOrgStats = new _GithubOrgStats2.default(GITHUB_TOKEN, GITHUB_ORGANIZATION);

githubOrgStats.get().then(function (result) {
  return console.log(result);
});