'use strict';

var _path = require('path');

var _path2 = _interopRequireDefault(_path);

var _fs = require('fs');

var _fs2 = _interopRequireDefault(_fs);

var _sassLint = require('sass-lint');

var _sassLint2 = _interopRequireDefault(_sassLint);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

process.on('message', function (assets) {
  var scssLintPath = _path2.default.resolve(process.cwd(), '.sass-lint.yml');
  try {
    _fs2.default.accessSync(scssLintPath, _fs2.default.F_OK);
  } catch (e) {
    console.warn('.sass-lint.yml not found. skipping lint.');
    process.exit(0);
  }

  var result = _sassLint2.default.lintFiles(assets, {}, scssLintPath);
  _sassLint2.default.outputResults(result, {}, scssLintPath);
  var failed = _sassLint2.default.resultCount(result);

  if (failed) {
    process.exit(1);
  } else {
    process.exit(0);
  }
});

/**
* NPM dependencies
**/
/**
* Node dependencies
**/